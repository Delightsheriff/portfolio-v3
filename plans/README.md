# Implementation Plans

Generated by the improve skill on 2026-07-11. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix `useMemo` side-effect in ProjectFilter | P1 | S | — | TODO |
| 002 | Consolidate duplicate ClickSparkContext definitions | P1 | S | — | TODO |
| 003 | Add missing `openToWork` field to hero GROQ query | P1 | S | — | TODO |
| 004 | Enable Sanity CDN in production | P1 | S | — | TODO |
| 005 | Add runtime validation for required environment variables | P2 | S | — | TODO |

## Dependency notes

- Plans 001–005 are all independent and can be executed in any order.
- Plan 005 touches `sanity/sanity.client.ts`, which plan 004 also touches. If both are being applied, apply 004 first and then 005 (005 includes the `useCdn` change in its scope). If only one is being applied, the other's change is not affected.

## Findings considered and rejected

- **React.memo on frequent renders**: Not worth the complexity for a portfolio with small datasets. Revisit if performance issues are measured.
- **Framer-motion cursor spring optimization**: The cursor is a design feature, not a bottleneck. Not worth optimizing unless jank is measured on target devices.
- **Unused dependency removal (`styled-components`, `@tanstack/table-core`, etc.)**: Worth doing, but produces no user-facing change. Defer to a separate maintenance pass.
- **`CATEGORY_LABELS` deduplication**: Trivial improvement that adds a new file and import changes across 4 components. Worth doing but not urgent.
- **Splitting large files (`project-page.tsx` at 660 lines)**: Would improve maintainability but is a refactor with risk of breaking the complex layout. Defer until tests exist.
- **Pre-commit hooks setup**: Good practice, but this is a solo project with no external contributors. Lower priority than bugs.
- **ESLint flat config migration**: The current compat layer works. Not worth the migration churn.
- **A11y audit beyond static code**: Would require manual screen reader testing. Flagged for a future dedicated pass.
